package round;

import java.util.LinkedList;

import golfIreland.GameState;
import menu.StartMenu;

public class recordHoleTester {
	private static boolean upanddown;

	public static void main(String[] args) {
		
		/*StartMenu start = new StartMenu();
		if(start.getHasQuit()){
			return;
		}

		GameState gameState = start.getGameState();
		 //round test = new round("Port", 71, "Whites", 3, "07/12/2022", gameState);
		 //test.fillRound();
		 //test.getStats();
		/*
		recordShot test = new recordShot(1, 400, "Driver", 200, "fairway");
		recordShot test1 = new recordShot(2, 200, "7Iron", 20, "semi rough");
		recordShot test2 = new recordShot(3, 20, "Lob Wedge", 2, "Green");
		recordShot test3 = new recordShot(4, 2, "puuter", 0, "Hole");
		
		LinkedList<recordShot> shots = new LinkedList<>();
		shots.add(test);
		shots.add(test1);
		shots.add(test2);
		shots.add(test3);
		
		int hole = shots.size() - 3;
		System.out.println(shots.get(hole).toString());
		if (shots.get(hole).getEndPosition() != "Green") {
			 upanddown = true;
		} else {
			upanddown = false;
		}
		
		System.out.println(upanddown);
		 */

	}

}
